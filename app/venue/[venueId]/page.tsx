'use client';

import React from 'react';
import { VenueDetail } from '@/app/components/venue/VenueDetail';

export default function VenueDetailPage() {
  // The VenueDetail component itself handles fetching data based on URL params
  return <VenueDetail />;
} 